export enum CarouselType {
  NORMAL,
  LOOP,
  AUTOMATIC,
}

export enum PaginationType {
  NONE,
  PAGE_NUMBER,
  PAGE_NUMBER_TOTAL,
}

export enum CarouselControlPosition {
  DEFAULT,
  FAR_ENDS,
  MID_SCREEN,
}
